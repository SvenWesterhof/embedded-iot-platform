/**
 * @file credentials.h.template
 * @brief Template for sensitive credentials
 * 
 * INSTRUCTIONS:
 * 1. Copy this file to 'credentials.h' in the same directory
 * 2. Fill in your actual values
 * 3. Never commit credentials.h to version control
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// ============================================================================
// WiFi Credentials
// ============================================================================

#define WIFI_SSID           "your_wifi_ssid"
#define WIFI_PASSWORD       "your_wifi_password"

// ============================================================================
// MQTT Broker Credentials
// ============================================================================

#define MQTT_BROKER_URI     "insert_broker_url"
#define MQTT_CLIENT_ID      "embedded-iot-gateway"   // Must match AWS IoT Core policy
#define MQTT_USERNAME       NULL                    // NULL if not required
#define MQTT_PASSWORD       NULL                    // NULL if not required
#define MQTT_TOPIC_PREFIX   "esp32_gateway"

// ============================================================================
// NTP Configuration
// ============================================================================

#define NTP_SERVER          "pool.ntp.org"
#define NTP_TIMEZONE_OFFSET 0                       // UTC offset in hours

// ============================================================================
// Device Identification
// ============================================================================

#define DEVICE_NAME         "ESP32_Gateway_01"
#define DEVICE_LOCATION     "Unknown"


// TLS certificates embedded via EMBED_TXTFILES in Application/CMakeLists.txt
// cppcheck-suppress syntaxError
extern const unsigned char amazon_root_ca_pem_start[]  asm("_binary_AmazonRootCA1_pem_start");
// cppcheck-suppress syntaxError
extern const unsigned char device_cert_pem_start[]      asm("_binary_device_cert_pem_start");

#endif // CREDENTIALS_H
