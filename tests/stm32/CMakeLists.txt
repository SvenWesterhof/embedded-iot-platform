# STM32-specific host-native unit tests
# Add tests here that cover STM32 middleware/application logic
# that can be compiled without the ARM GCC toolchain.
#
# Example:
#   add_unity_test(test_protocol_handler test_protocol_handler.c)
